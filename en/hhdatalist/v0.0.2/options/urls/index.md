# urls

<table class="options-table">
  <tr>
    <th>Necessity:</th>
    <td>Required and exclusive with <code>url</code></td>
  </tr>
</table>

The `urls` option ...

``` js nonum
new HHDataList({
  urls: '',
});
```

