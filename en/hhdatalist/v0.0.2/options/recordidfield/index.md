# recordIdField

<table class="options-table">
  <tr>
    <th>Necessity:</th>
    <td>Expected</td>
  </tr>
</table>

The `recordIdField` option ...

``` js nonum
new HHDataList({
  recordIdField: '',
});
```

